import { useSpring, animated as a } from 'react-spring';
const MysteriousText = ({ children, ...props }) => {
    // const useAnimation = i =>
    //     useSpring({ opacity: 1, from: { opacity: 0 }, delay: Math.random() * 350 + 500, duration: 5000 });
    // return children.split("").map((item, index) => (
    //     <a.span key={index} style={useAnimation(index)} {...props}>
    //         {item}
    //     </a.span>
    // ));
};

export { MysteriousText  };